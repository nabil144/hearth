name: Hearth
options:
  bundleIdPrefix: com.nabil
  deploymentTarget:
    iOS: "26.0"
  xcodeVersion: "26.0"
  generateEmptyDirectories: true
  createIntermediateGroups: true

packages:
  HearthEngine:
    path: Engine

settings:
  base:
    SWIFT_VERSION: "6.0"
    SWIFT_STRICT_CONCURRENCY: minimal
    MARKETING_VERSION: "0.1"
    CURRENT_PROJECT_VERSION: "1"
    CODE_SIGN_STYLE: Automatic
    DEVELOPMENT_TEAM: ""
    ENABLE_USER_SCRIPT_SANDBOXING: true

targets:
  Hearth:
    type: application
    platform: iOS
    sources:
      - path: ios/Sources
      - path: content/greek.json
        type: file
        buildPhase: resources
      - path: web/stills
        type: folder
        buildPhase: resources
      - path: web/audio
        type: folder
        buildPhase: resources
      - path: content/films.json
        type: file
        buildPhase: resources
      - path: web/shorts/greek-04-poc.mp4
        type: file
        buildPhase: resources
      - path: web/shorts/became-father-imagegen-v1.mp4
        type: file
        buildPhase: resources
    dependencies:
      - package: HearthEngine
    info:
      path: ios/Info.plist
      properties:
        CFBundleDisplayName: Hearth
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        UILaunchScreen: {}
        UISupportedInterfaceOrientations: [UIInterfaceOrientationPortrait]
        UIBackgroundModes: [audio]
        ITSAppUsesNonExemptEncryption: false
        NSLocalNetworkUsageDescription: Hearth plays tonight's film from your computer on the local network.
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: true
          NSAllowsLocalNetworking: true
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.nabil.Hearth
        TARGETED_DEVICE_FAMILY: "1"
        INFOPLIST_KEY_UIApplicationSceneManifest_Generation: true
